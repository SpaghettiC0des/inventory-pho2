;(function ( w, $ , ko ) {
	"use strict";
	var _base = w.INVENTO.baseURL, xhr = {

		post : function ( url, data ) {
			return $.post( _base + url, data );
		},
		get : function ( url) {
			return $.get( url );
		},
		getJ : function ( url) {
			return $.getJSON( url );
		},
	};

	w.INVENTO.XHR = xhr;
}( window, jQuery, ko ));